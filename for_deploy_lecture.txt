1. Запускаем и проверяем проект, чтобы все работало
2. Устанавливаем локально json-server command: npm i json-server
3. Перейдем на heroku cli(рассказать снова про сервер, что это, зачем нужно)
4. Регистрируемся на хероку и заходим в свой профиль(подтверждение приходит на почту, скорее всего попадет в спам)
5. Скачиваем хироку для мака отдельно, для винды отдельно, после установки, установим сам хироку глобально(npm install -g heroku)
Проверить скачался или нет(heroku --version)
6. Логин в хироку(heroku login)(после логина едем дальше)
7. Создаем файлы server.js & Procfile(добавляем настройки и пройдемся по ним)
8. Затем переходим на сайт хироку в свой профиль(нужен дашборд, создать новое приложение)
9. Заливаем весь проект на гитхаб(здесь рассказать о том что в большинстве случаев практически всегда сервак питается из исходного кода, который лежит в гитхабе где-нибудь)
10. После этого необходимо синхронризировать хироку и гитхаб, для этого просто дадим разрешение чтобы хироку приконнектился к гитхабу
11. Далее находим нужный репозиторий и нажимаем коннект
12. После подключения просто нажимаем внизу деплой бранч(там можно выбирать какую ветку забрэнчить, но так как у нас мастер по умолчанию выбираем его)
13. Проект задеплоится успешно, но возникнет такая проблема, мы не сможем видеть товары, то есть все прогрузилось, но товаров нет(происходит из-за того, что у нас просто неправильное обращение к жсон серверу(идем в файл, который лежит в хелперсах(конкретно интересует ссылка на жсон-сервер, просто изменим ее)))
14. Затем необходимо перейти в package.json и изменить скрипты(вместо react scripts start добавим node server.js) после изменений все пушим снова в гитхаб
15. Обновляем сервер хироку(снова жмякаем деплой бранч) после этого все должно работать
16. Напоминаю что регистрация работать не будет, так как бэк на который отправляется запрос не защищен